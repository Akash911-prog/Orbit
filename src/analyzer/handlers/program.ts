import type { OrbType } from '../../types';
import type { AnalyzerContext } from '../context';

export function handleProgram(node: any, ctx: AnalyzerContext): OrbType {
    throw new Error('Method not implemented.');
}
